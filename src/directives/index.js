import dirOn from './on';
import dirText from './text';
import dirModel from './model';
import component from './component';
import prop from './prop';

export default {
  on: dirOn,
  text: dirText,
  model: dirModel,
  component,
  prop,
};